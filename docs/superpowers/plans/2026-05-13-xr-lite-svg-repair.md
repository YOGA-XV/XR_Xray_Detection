# XR-Lite SVG Repair Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rebuild the XR-Lite architecture figure as a corrected, fully editable SVG that matches the current `yolov8n-xr-lite.yaml` topology.

**Architecture:** Create a small deterministic SVG generation script dedicated to the corrected XR-Lite figure, then emit the final `XR-Lite-corrected.svg` asset from that script. Verify the resulting SVG by XML parsing, structural label checks, and visual rendering inspection against the approved topology requirements.

**Tech Stack:** Python 3 standard library, hand-authored SVG primitives, PowerShell validation commands.

---

## File Structure

- Create `G:\XR_Xray_Detection\papers\figures\build_xr_lite_corrected_svg.py`
  - Single-purpose generator for the repaired XR-Lite SVG.
  - Defines reusable helpers for text, boxes, arrows, and layout sections.
  - Encodes corrected PAN-FPN connectivity explicitly.
- Create `G:\XR_Xray_Detection\papers\figures\XR-Lite-corrected.svg`
  - Final editable deliverable generated by the script.
- Reuse `G:\XR_Xray_Detection\ultralytics\cfg\models\v8\yolov8n-xr-lite.yaml`
  - Truth source for topology validation only; no edits.

### Task 1: Build the deterministic SVG generator

**Files:**
- Create: `G:\XR_Xray_Detection\papers\figures\build_xr_lite_corrected_svg.py`

- [ ] **Step 1: Define the SVG scaffold and reusable drawing helpers**

```python
from pathlib import Path
from xml.sax.saxutils import escape

OUT = Path(__file__).with_name("XR-Lite-corrected.svg")

def rect(x, y, w, h, fill, stroke, rx=12, stroke_width=2):
    return (
        f'<rect x="{x}" y="{y}" width="{w}" height="{h}" rx="{rx}" '
        f'fill="{fill}" stroke="{stroke}" stroke-width="{stroke_width}"/>'
    )

def text(x, y, value, size=18, fill="#111827", weight=500, anchor="middle"):
    return (
        f'<text x="{x}" y="{y}" font-size="{size}" font-weight="{weight}" '
        f'fill="{fill}" text-anchor="{anchor}" font-family="Arial, Helvetica, sans-serif">'
        f'{escape(value)}</text>'
    )
```

- [ ] **Step 2: Add helpers for arrows and multi-line module labels**

```python
def line(points, stroke="#111827", width=3, dashed=False, marker=True):
    coords = " ".join(f"{x},{y}" for x, y in points)
    dash = ' stroke-dasharray="8 6"' if dashed else ""
    end = ' marker-end="url(#arrow)"' if marker else ""
    return f'<polyline points="{coords}" fill="none" stroke="{stroke}" stroke-width="{width}"{dash}{end}/>'

def module_box(x, y, w, h, title, subtitle, fill, stroke):
    return [
        rect(x, y, w, h, fill, stroke),
        text(x + w / 2, y + 28, title, 18, stroke, 700),
        text(x + w / 2, y + 54, subtitle, 13, "#4b5563", 500),
    ]
```

- [ ] **Step 3: Encode the four panel layout and figure title**

```python
parts = [
    '<svg xmlns="http://www.w3.org/2000/svg" width="1800" height="1220" viewBox="0 0 1800 1220">',
    '<defs><marker id="arrow" markerWidth="10" markerHeight="10" refX="8" refY="5" orient="auto"><path d="M0,0 L10,5 L0,10 z" fill="#111827"/></marker></defs>',
    rect(0, 0, 1800, 1220, "#ffffff", "#ffffff", 0, 0),
    text(900, 60, "XR-Lite Architecture", 34, "#111827", 700),
]
```

### Task 2: Encode the corrected XR-Lite topology

**Files:**
- Modify: `G:\XR_Xray_Detection\papers\figures\build_xr_lite_corrected_svg.py`

- [ ] **Step 1: Add preprocessing and backbone modules**

Create editable boxes and arrows for:

```text
Pseudo-color X-ray RGB -> PCN -> EGI -> 4-channel XR Input
4-channel XR Input -> P1/2 -> P2/4 -> P3/8 -> P4/16 -> P5/32
```

Include the real enhanced backbone labels:

```text
P4 / 16 -> C2fEMALite x6
P5 / 32 -> SPPFAIFILite
```

- [ ] **Step 2: Add the corrected top-down PAN-FPN chain**

Draw this exact sequence:

```text
P5 / 32 (AIFI-enhanced) -> Upsample x2
Upsample x2 + P4 / 16 (Enhanced) -> Concat
Concat -> C2f x3
C2f x3 -> Upsample x2
Upsample x2 + P3 / 8 -> Concat
Concat -> C2f x3
C2f x3 -> Neck P3 / 8
```

- [ ] **Step 3: Add the corrected bottom-up PAN chain and detect branches**

Draw this exact sequence:

```text
Neck P3 / 8 -> Detect P3 / 8
Neck P3 / 8 -> Conv 3x3, s=2
Conv 3x3, s=2 + P4 top-down intermediate -> Concat
Concat -> C2fEMALite x3 -> Neck P4 / 16 -> Detect P4 / 16
Neck P4 / 16 -> Conv 3x3, s=2
Conv 3x3, s=2 + P5 / 32 (AIFI-enhanced) -> Concat
Concat -> C2fEMALite x3 -> Neck P5 / 32 -> Detect P5 / 32
```

- [ ] **Step 4: Add footer legend and method formula**

Add editable text explaining:

```text
Backbone: YOLOv8n-XR-Lite
Neck: corrected PAN-FPN fusion
Detection Head: P3 / P4 / P5
XR-Lite = PCN + EGI + EMA-Lite + AIFI-Lite + YOLOv8n
```

### Task 3: Generate and verify the SVG deliverable

**Files:**
- Create: `G:\XR_Xray_Detection\papers\figures\XR-Lite-corrected.svg`

- [ ] **Step 1: Run the generator**

Run:

```powershell
python G:\XR_Xray_Detection\papers\figures\build_xr_lite_corrected_svg.py
```

Expected:

```text
Creates G:\XR_Xray_Detection\papers\figures\XR-Lite-corrected.svg
```

- [ ] **Step 2: Verify the SVG is valid XML**

Run:

```powershell
@'
import xml.etree.ElementTree as ET
ET.parse(r"G:\XR_Xray_Detection\papers\figures\XR-Lite-corrected.svg")
print("SVG XML OK")
'@ | python -
```

Expected:

```text
SVG XML OK
```

- [ ] **Step 3: Verify key corrected labels are present**

Run:

```powershell
rg -n "Neck P3 / 8|Neck P4 / 16|Neck P5 / 32|C2fEMALite x3|Detect P3 / 8|Detect P4 / 16|Detect P5 / 32" G:\XR_Xray_Detection\papers\figures\XR-Lite-corrected.svg
```

Expected:

```text
Matches for all required repaired topology labels
```

- [ ] **Step 4: Render a browser preview for visual inspection**

Run:

```powershell
Start-Process G:\XR_Xray_Detection\papers\figures\XR-Lite-corrected.svg
```

Expected:

```text
The SVG opens cleanly and the corrected top-down / bottom-up arrows are readable.
```

## Plan Self-Review

- Spec coverage:
  - Corrected PAN-FPN topology: Task 2.
  - Editable SVG output: Tasks 1 and 3.
  - Preserve overall visual structure: Task 2.
  - No XR-Plus scope expansion: Entire plan stays XR-Lite only.
- Placeholder scan:
  - No `TODO`, `TBD`, or unspecified implementation placeholders remain.
- Type consistency:
  - The plan uses one generator script, one SVG output file, and repeated helper names consistently.
